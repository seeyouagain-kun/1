# Python 3.9
numpy>=1.20.0
gymnasium>=0.29.0
torch>=2.0.0
skrl>=1.0.0
vpython>=7.6.0
tensorboard>=2.10.0

# PEARL Meta-RL 依赖（纯 PyTorch 实现，无需额外框架）
# PEARL Meta-RL dependencies (pure PyTorch implementation, no extra frameworks needed)
# torch already covers SAC + context encoder implementation
